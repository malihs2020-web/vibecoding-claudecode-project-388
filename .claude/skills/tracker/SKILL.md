---
name: tracker
description: |
  Собирает цены на конкретный товар (Premier, ягнёнок с индейкой для собак средних пород, 10 кг) из нескольких источников и оформляет единую таблицу прогона (одна строка на источник). Источники: фиксированный список URL сторонних магазинов (см. ниже) — всегда; плюс Ozon, Яндекс Маркет и Wildberries — если доступен браузер. Переиспользует skill extract-price для самого извлечения цены с каждой страницы. После сборки таблицы сохраняет прогон как YYYY-MM-DD.json в приватный репозиторий malihs2020-web/tracker-data через GitHub MCP (отдельный коммит на файл, без локального git). Используй, когда нужно отследить/сравнить цену этого товара по всем источникам сразу.

  Фиксированный список URL для обхода:
  https://petsmart.ru/product/10152-premier-adult-medium-korm-s-yagnenkom-i-indejkoj-dlya-sobak-srednix-porod-10-kg
  https://zoozavr.ru/product/index/id/6189303/
  https://kotmatros.ru/premier-dog-lamb-turkey-adult-medium-suhoj-korm-dlya-sobak-srednih-porod-svezhee-myaso-yagnenka-s-indejkoj-10-kg/
  https://glavnoehvost.ru/katalog/sobaki/korma-dla-sobak/suhie-korma/premier-dla-sobak-adult-medium-agnenok-indejka/9196
  https://www.zoogastronom.ru/product/suhoy-korm-premier-yagnenok-s-indeykoy-dlya-sobak-srednih-porod
  https://katiko.ru/premier-yagnenka-indejkoj-sobak-krupnyh-10
  https://www.dogeat.ru/catalog/korm/dlya-sobak/premier/
  https://premier.pet/catalog/dog_foods/medium_breeds/pr_adult_medium_lamb_turkey_1_kg/
  https://multikorm.ru/category/sobaki/korma-dlya-sobak/sukhoy-korm-dlya-sobak/premier-sukhoy-korm-dlya-sobak/premier-sukhoy-korm-dlya-sobak-10-kg/
---

# Tracker

Собирает цены на товар из нескольких источников и оформляет единую таблицу прогона (одна строка на источник). Извлечение цены с конкретной страницы не дублируется здесь — на каждом URL переиспользуется skill `extract-price` (раздел «Порядок действий»). `tracker` отвечает за то, **откуда** брать URL (фиксированный список + маркетплейсы +, при необходимости, fallback-поиск) и за сведение результатов в таблицу; `extract-price` — за то, **как** вытащить цену с уже данной страницы.

## Источники

1. **Фиксированный список URL** (см. `description` выше и раздел ниже) — проверяется всегда, браузер не требуется.
2. **Ozon / Яндекс Маркет / Wildberries** — проверяются дополнительно, если доступен браузер (эти три сайта блокируют прямые запросы, без браузера к ним не подступиться).
3. **Fallback-поиск сторонних магазинов** — вспомогательный способ добрать источники сверх фиксированного списка (например, если по товару из фиксированного списка нужно больше точек сравнения, или нужно повторить прогон для другого товара, для которого фиксированного списка ещё нет).

## Шаг 0: проверка браузера

1. Вызвать `tabs_context_mcp` (при необходимости с `createIfEmpty: true`).
2. Успешный ответ со списком вкладок → браузер **доступен** → дополнительно к фиксированному списку обработать Ozon / Яндекс Маркет / Wildberries.
3. Ошибка («Browser extension is not connected» и т.п.) → браузер **недоступен** → обрабатывать только фиксированный список URL (и, при необходимости, fallback-поиск сторонних магазинов — он тоже не требует браузера).

## Фиксированный список URL для обхода

1. https://petsmart.ru/product/10152-premier-adult-medium-korm-s-yagnenkom-i-indejkoj-dlya-sobak-srednix-porod-10-kg
2. https://zoozavr.ru/product/index/id/6189303/
3. https://kotmatros.ru/premier-dog-lamb-turkey-adult-medium-suhoj-korm-dlya-sobak-srednih-porod-svezhee-myaso-yagnenka-s-indejkoj-10-kg/
4. https://glavnoehvost.ru/katalog/sobaki/korma-dla-sobak/suhie-korma/premier-dla-sobak-adult-medium-agnenok-indejka/9196
5. https://www.zoogastronom.ru/product/suhoy-korm-premier-yagnenok-s-indeykoy-dlya-sobak-srednih-porod
6. https://katiko.ru/premier-yagnenka-indejkoj-sobak-krupnyh-10
7. https://www.dogeat.ru/catalog/korm/dlya-sobak/premier/
8. https://premier.pet/catalog/dog_foods/medium_breeds/pr_adult_medium_lamb_turkey_1_kg/
9. https://multikorm.ru/category/sobaki/korma-dlya-sobak/sukhoy-korm-dlya-sobak/premier-sukhoy-korm-dlya-sobak/premier-sukhoy-korm-dlya-sobak-10-kg/

## Общее правило точного совпадения (для поисковых источников: Ozon, Яндекс Маркет, Wildberries, fallback-поиск)

Поиск/сортировка по цене на маркетплейсах и в поиске смешивает разные модификации товара (другой вкус, вес, размер/порода, объём упаковки). Среди найденных/отсортированных по возрастанию цены вариантов нужно оставлять для сравнения только те, чьё название/характеристики **точно** соответствуют запрошенному товару — даже если менее подходящий вариант дешевле, его нужно пропустить. Если характеристика не даёт однозначно понять соответствие по тексту (например, вес), проверять по изображению упаковки (zoom скриншота карточки) или по описанию на странице товара.

## Порядок действий: фиксированный список URL

1. Взять список URL выше (не искать и не подбирать источники самостоятельно — список фиксированный).
2. Для каждого URL по очереди вызвать skill `extract-price` (получить объект `{ regular_price, sale_price }`, либо `товар не найден` с причиной).
3. Не переопределять и не копировать логику парсинга цены внутри `tracker` — она берётся из `extract-price` как есть.

## Порядок действий: Ozon (только если доступен браузер)

Ozon блокирует прямые запросы (WebFetch/curl получают петлю редиректов через анти-бот защиту) — работать можно только через реальный браузер (Claude in Chrome).

1. Открыть ozon.ru, ввести в поиск название товара (бренд + категория + ключевые характеристики).
2. Применить доступные фильтры под запрошенные характеристики: бренд, вес (например, диапазон 10000–20000 г для «10 кг»), и т.п.
3. Включить сортировку «Дешевле» (в URL параметр `sorting=price`).
4. Считать список карточек (`get_page_text` и/или `read_page`): название, цена, наличие зачёркнутой (старой) цены.
5. Применить общее правило точного совпадения — отфильтровать карточки, не соответствующие запрошенным характеристикам.
6. Взять топ-N самых дешёвых точных совпадений. Для каждого получить прямую ссылку на карточку (`find` по названию/цене → `read_page` по `ref_id` → атрибут `href`).

## Порядок действий: Яндекс Маркет (только если доступен браузер)

Тоже требует браузера — WebFetch/curl аналогично блокируются или не отдают цену (JS-рендеринг).

1. Открыть `market.yandex.ru/search?text=<название товара>`.
2. При необходимости задать фильтры в панели слева (цена, бренд и т.п.).
3. Отдельного надёжного переключателя «сортировка по цене» на карточках маркетплейса может не быть — считывать список карточек через `get_page_text` (список подгружается частями, при необходимости повторять запрос / прокручивать страницу).
4. Применить общее правило точного совпадения.
5. Цена на карточке обычно в формате «Цена с картой Яндекс Пэй X ₽ вместо Y ₽»: `X` — это `sale_price` (цена с картой/акцией), `Y` — `regular_price`. Если указана только одна цена — это `regular_price`, `sale_price` = `null`.
6. Взять топ-N самых дешёвых точных совпадений, получить ссылку карточки (`find` → `read_page` по `ref_id` → `href`, ведёт на `/card/...`).

## Порядок действий: Wildberries (только если доступен браузер)

Тоже требует браузера.

1. Открыть `wildberries.ru/catalog/0/search.aspx?search=<название товара>`.
2. Открыть переключатель сортировки («По популярности») и выбрать «По возрастанию цены» (в URL добавляется `sort=priceup`).
3. Так как отдельного фильтра «вес» может не быть, а сортировка по цене смешивает разные объёмы упаковки (1 кг, 3 кг, 10 кг и т.д.) — задать в «Все фильтры» диапазон цены, отсекающий заведомо не подходящие по объёму варианты, либо проверять вес по каждой карточке.
4. Для карточек, где вес/вкус не виден в текстовом названии, — увеличить (zoom) скриншот карточки, чтобы прочитать вес/вкус с изображения упаковки.
5. Применить общее правило точного совпадения.
6. Взять топ-N самых дешёвых точных совпадений, получить ссылку карточки (`find` → `read_page` по `ref_id` → `href`, ведёт на `.../detail.aspx`).

## Порядок действий: fallback-поиск сторонних магазинов (вспомогательный, когда нужно расширить список)

Цель — набрать 5–10 рабочих ссылок на сторонние интернет-магазины (НЕ Ozon, НЕ Яндекс Маркет, НЕ Wildberries), по которым реально получается вытащить цену через `WebFetch`. Не требует браузера.

1. Вызвать `WebSearch` по названию/характеристикам товара, исключив домены `ozon.ru`, `market.yandex.ru`, `wildberries.ru` через `blocked_domains`.
2. Из результатов отобрать кандидатов — ссылки на конкретные карточки товара (не категории/обзоры/агрегаторы отзывов), по возможности разных магазинов.
3. Для каждого кандидата сделать пробный `WebFetch` с запросом на извлечение названия и цены.
4. Оставить только те ссылки, где `WebFetch` реально вернул читаемую цену — отсеять случаи блокировки (ошибка запроса, HTTP 4xx/498), пустого/усечённого контента и страниц, где цена подгружается через JS и не видна в ответе.
5. Применить общее правило точного совпадения к оставшимся результатам.
6. Если рабочих источников меньше 5 — при необходимости повторить `WebSearch` с уточнённым запросом. Если после разумного числа попыток рабочих источников нет вообще — сообщить об этом пользователю, не подставляя данные из сниппетов поиска вместо цены.
7. Полученные рабочие URL обработать так же, как фиксированный список (через `extract-price`).

## Сбор внутренней таблицы прогона (не показывать пользователю целиком)

Собрать результаты всех источников (фиксированный список + Ozon/ЯМ/WB, если проверялись + fallback-поиск, если использовался) в одну таблицу, по одной строке на источник — это внутренняя рабочая структура, нужна для расчёта `min_price`, для diff (см. ниже) и для сохранения в `tracker-data`, а не для прямого показа пользователю (см. «Итоговый вывод пользователю»):

| Источник (домен) | URL | regular_price | sale_price | Статус |
|---|---|---|---|---|
| petsmart.ru | https://petsmart.ru/... | 8260.90 | null | ok |
| glavnoehvost.ru | https://glavnoehvost.ru/... | — | — | не удалось получить цену (JS, цена не отображается в ответе) |

- «Источник» — домен из URL (или «Ozon» / «Яндекс Маркет» / «Wildberries» для карточек с этих площадок).
- `regular_price` / `sale_price` — значения из объекта, который вернул `extract-price` (или прочерк, если `товар не найден`).
- «Статус» — `ok`, либо краткая причина неудачи (без выдумывания цены вместо неё). Если товар не точно совпадает по характеристикам (см. общее правило точного совпадения) — отметить это тоже, а не молча включать в сравнение.

Из этой таблицы определить минимальную цену среди строк со статусом `ok` и с точным совпадением товара (сравнивать `sale_price`, если он есть, иначе `regular_price`) — она понадобится для `min_price` в JSON и для итогового вывода.

## Сравнение с предыдущим прогоном (diff)

Правила значимости изменений — в `KNOWLEDGE.md` (тот же каталог skill'а). Перед их применением прочитать этот файл.

1. Найти в репозитории `malihs2020-web/tracker-data` последний по дате файл `YYYY-MM-DD.json` **до** даты текущего прогона (список файлов — GitHub `contents` API/MCP по корню репозитория). Если файлов ещё нет — diff не строится, это первый прогон (см. «Итоговый вывод пользователю»).
2. Сопоставить источники текущего и предыдущего прогона по полю `source` (+ `url`, если у одного `source` несколько строк, как у Ozon/ЯМ/WB).
3. Для каждой пары применить правила значимости из `KNOWLEDGE.md`:
   - существенное изменение цены (порог ±2% и одновременно строго больше 50 ₽) — старая и новая цена;
   - появление/исчезновение скидки — независимо от порогов;
   - источник, которого не было в предыдущем прогоне (и он `ok` сейчас) — «новый товар»;
   - источник, который был `ok` в предыдущем прогоне, а сейчас отсутствует/не `ok` — «товара нет».
4. Незначимые колебания (≤50 ₽ и/или <2%) — отбросить, в diff не включать.

## Итоговый вывод пользователю

После прогона пользователю показывается **только список значимых изменений** из diff — не сырая таблица со всеми ценами по всем источникам:

- Значимые изменения есть → вывести их списком/таблицей: источник, было → стало, тип изменения (рост/падение цены, появилась/пропала скидка, новый товар, товара нет). Прочие источники без значимых изменений в вывод не включать.
- Значимых изменений нет (все колебания ≤50 ₽/<2%, скидки без изменений, состав источников не менялся) → одна строка: значимых изменений цены не найдено, без пустой таблицы.
- Первый прогон (нет предыдущего файла для сравнения, diff не строится) → сравнивать не с чем, поэтому вместо diff вывести одну строку с минимальной ценой (`min_price`) и явно отметить, что это первый прогон и сравнение появится со следующего.

Полная таблица по всем источникам в чат не выводится — она остаётся только внутри JSON, сохранённого в `tracker-data`.

## Сохранение прогона в tracker-data

После того как таблица собрана, `tracker` сохраняет её как данные (без кода) в отдельный приватный репозиторий `malihs2020-web/tracker-data` — один прогон = один файл `YYYY-MM-DD.json` в корне репозитория, имя файла — дата прогона. Запись идёт напрямую через GitHub MCP (`create_or_update_file`), это сразу отдельный коммит в репозитории — локальный `git clone`/`git push` не используется.

### Формат файла

```json
{
  "date": "2026-07-20",
  "product": "Premier, ягнёнок с индейкой, для собак средних пород, 10 кг",
  "results": [
    { "source": "petsmart.ru", "url": "https://petsmart.ru/...", "regular_price": 8260.90, "sale_price": null, "status": "ok", "exact_match": true },
    { "source": "glavnoehvost.ru", "url": "https://glavnoehvost.ru/...", "regular_price": null, "sale_price": null, "status": "не удалось получить цену (JS, цена не отображается в ответе)", "exact_match": null }
  ],
  "min_price": { "source": "wildberries", "url": "...", "price": 7017 }
}
```

- `results` — построчно та же таблица прогона, по одному объекту на источник: `source` (домен либо «Ozon» / «Яндекс Маркет» / «Wildberries»), `url`, `regular_price`, `sale_price`, `status` (`"ok"` либо причина неудачи), `exact_match` (`true`/`false`/`null`, если неприменимо — например, при неудаче).
- `min_price` — источник и цена (сравнивалась `sale_price`, если есть, иначе `regular_price`) среди строк `status: "ok"` и `exact_match: true`; `null`, если таких строк нет.

### Порядок действий

1. Определить дату прогона в формате `YYYY-MM-DD` (дата запуска, локальная/системная).
2. Собрать JSON по формату выше из уже готовой таблицы прогона.
3. Проверить, существует ли уже файл `YYYY-MM-DD.json` в репозитории `malihs2020-web/tracker-data` (GitHub MCP `get_file_contents`, owner=`malihs2020-web`, repo=`tracker-data`, path=`YYYY-MM-DD.json`).
   - Не существует → перейти к шагу 4 без `sha`.
   - Существует (повторный прогон в тот же день) → запомнить `sha` файла из ответа — он понадобится для обновления.
4. Записать файл через GitHub MCP `create_or_update_file`: owner=`malihs2020-web`, repo=`tracker-data`, path=`YYYY-MM-DD.json`, branch=`main`, content = JSON из шага 2, `sha` — если файл уже существовал (шаг 3), commit message вида `Run YYYY-MM-DD`.
5. Если запись не удалась (например, 403/404 — нет доступа GitHub MCP к репозиторию `tracker-data`, или репозиторий/ветка не существует) — не считать это ошибкой всего прогона: итоговый вывод пользователю (см. «Итоговый вывод пользователю») уже показан, но явно сообщить, что сохранение в `tracker-data` не удалось, и с чем это связано (без повторных попыток другими способами, например через `git push` в обход MCP — таким способом коммитить в `tracker-data` в рамках этого skill не нужно).
